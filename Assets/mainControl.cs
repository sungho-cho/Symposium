using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mainControl : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //this.transform.position = GameObject.Find("Character").transform.position;
        //this.transform.rotation.Set(this.transform.rotation.x, GameObject.Find("Character").transform.rotation.y, this.transform.rotation.z, this.transform.rotation.w);
    }
}
